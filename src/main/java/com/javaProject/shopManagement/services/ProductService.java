package com.javaProject.shopManagement.services;

public class ProductService {

       /* private ProductDAO productDAO;

        public List<Product> getAllProducts() {

            return productDAO.getInstance().getAll();
        }

        public Product getProductById(int id) {
            return productDAO.getInstance().getById(id);
        }

        public void addProduct(Product product) {
            productDAO.getInstance().add(product);
        }

        public void updateProduct(Product product) {
            productDAO.getInstance().update(product);
        }

        public void getByCondition(String condition) {
            productDAO.getInstance().getByCondition(condition);
        }

        public void deleteProduct(int id) {
            productDAO.delete(id);
        }*/
    }

